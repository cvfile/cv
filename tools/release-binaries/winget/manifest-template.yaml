# WinGet package manifest template for the `cv` CLI.
# Three files are required by the WinGet schema (version, installer, locale).
# This is the version manifest; the installer + locale manifests live alongside.
# Submit to https://github.com/microsoft/winget-pkgs after each release.

PackageIdentifier: cvfile.cv
PackageVersion: __VERSION__
DefaultLocale: en-US
ManifestType: version
ManifestVersion: 1.6.0
