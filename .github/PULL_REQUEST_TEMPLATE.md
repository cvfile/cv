## What

One or two sentences: what does this PR change and why? Replace this line.

## Checklist

- [ ] `pnpm build` and `pnpm test` pass locally (plus `pytest` in `sdks/python` and `go test ./...` in `sdks/go` if those SDKs are touched)
- [ ] `pnpm lint` passes
- [ ] `CHANGELOG.md` updated for user-facing changes
- [ ] No generated files committed (`dist/`, `coverage/`, build artifacts)
- [ ] Spec changes (if any) include rationale and updated `spec/test-vectors/`
