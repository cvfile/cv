name: publish cvfile-cv-detector (PyPI)

on:
  push:
    tags: ["cvfile-cv-detector-py/v*"]

permissions:
  contents: read
  # Required for PyPI Trusted Publisher (no token needed once configured at
  # https://pypi.org/manage/account/publishing/).
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - name: Install uv
        run: pipx install uv
      - name: Run tests before publishing
        # `uv run` provisions the project plus its dev dependency group
        # (pytest) on the fly; no explicit venv step needed.
        working-directory: tools/cv-detector/python
        run: uv run pytest -q
      - name: Install build tools
        run: python -m pip install --upgrade build
      - name: Build wheel + sdist
        working-directory: tools/cv-detector/python
        run: python -m build
      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: tools/cv-detector/python/dist
