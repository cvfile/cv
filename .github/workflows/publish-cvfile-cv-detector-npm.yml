name: publish cvfile-cv-detector (npm)

on:
  push:
    tags: ["cvfile-cv-detector-ts/v*"]

permissions:
  contents: read
  # Required for npm OIDC provenance attestations.
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v6
      - uses: actions/setup-node@v4
        with:
          # Node 24 ships npm >= 11.5.1 (OIDC trusted publishing). No registry-url
          # (its empty _authToken placeholder would suppress OIDC).
          node-version: 24
      - name: Install dependencies
        working-directory: tools/cv-detector/typescript
        # No --ignore-workspace: this package has its own pnpm-workspace.yaml
        # (with the esbuild build approval pnpm 11 requires); --ignore-workspace
        # would skip it and fail with ERR_PNPM_IGNORED_BUILDS.
        run: pnpm install --frozen-lockfile
      - name: Build
        working-directory: tools/cv-detector/typescript
        run: pnpm build
      - name: Test before publishing
        working-directory: tools/cv-detector/typescript
        run: pnpm test
      - name: Publish
        working-directory: tools/cv-detector/typescript
        # Publish with the npm CLI (>= 11.5.1) so npm's OIDC trusted publishing
        # is used; `pnpm publish`'s token exchange 404s against npm. This package
        # has no workspace: deps, so npm can pack from source directly.
        run: |
          echo "node $(node -v), npm $(npm -v), oidc=${ACTIONS_ID_TOKEN_REQUEST_URL:+present}"
          npm publish --access public --provenance
