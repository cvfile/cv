"""Optional integrations with third-party AI tooling.

Each submodule lazy-imports its host library so installing `cvfile` alone
does not pull LangChain/LlamaIndex/etc. as transitive dependencies.
"""
